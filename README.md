# voice_assistant_bert
A voice-controlled AI assistant built with PyTorch and BERT for intent recognition. It listens to user speech, classifies intents using a fine-tuned BERT model (on SNIPS + custom commands), and performs real actions like opening apps or telling the time.
